services:
  - type: web
    name: cmthakurclasses
    runtime: node
    buildCommand: cd server && npm install
    startCommand: node server/server.js
    envVars:
      - key: PORT
        value: 3000
